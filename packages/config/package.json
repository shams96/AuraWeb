{
  "name": "@chiarel/config",
  "version": "1.0.0",
  "description": "Configuration utilities for AuraBiosphere",
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "lint": "eslint src --ext .ts",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "zod": "^3.24.0",
    "@prisma/client": "^6.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "typescript": "^5.7.3",
    "vitest": "^3.0.0"
  }
}
